class Translator {
  constructor($translate) {
    this.$translate = $translate;
  }

  translate(list, translatePropertyName, childListPropertyName) {
    this.$location.url(url);
  }
}

export default Translator