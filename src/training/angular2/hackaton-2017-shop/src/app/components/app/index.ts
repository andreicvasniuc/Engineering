import { Component } from '@angular/core';

@Component({
  selector: 'app',
  templateUrl: 'template.html'
})

export class AppComponent {
  constructor() {
    //(<any>window).recoAPI.setCredentials('fUser2', 'Password2');
  }
}
